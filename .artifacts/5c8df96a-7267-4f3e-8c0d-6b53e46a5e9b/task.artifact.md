# مهام تنفيذ نظام التصاريح الديناميكي للضيوف

- `[x]` تحديث قاعدة البيانات
    - `[x]` إضافة عمود `allowed_sections` لجدول `profiles`
    - `[x]` تحديث دوال وصلاحيات RLS للتحقق من الأقسام المسموحة
- `[x]` تحديث لوحة الإدارة (`admin.tsx`)
    - `[x]` إضافة واجهة اختيار الصلاحيات (Checkboxes) لكل عضو برتبة ضيف
    - `[x]` ربط الحفظ بقاعدة البيانات
- `[x]` تحديث الهيكل العام والملاحة (`app-shell.tsx`)
    - `[x]` تعديل منطق عرض القوائم ليعتمد على `allowed_sections`
    - `[x]` تحسين التوجيه التلقائي للضيوف
- `[ ]` التحقق والرفع
    - `[ ]` تجربة تخصيص صلاحيات لضيف والتحقق من النتيجة
    - `[ ]` رفع كافة التعديلات لـ GitHub
